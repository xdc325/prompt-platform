# Changelog

## 2026-08-04 — 版本创建体验优化

### 解决的问题

#### 1. 基版本选择（parent_version_id）

**问题**：创建新版本时，无法选择基于哪个已有版本进行修改。系统默认以"当前已发布版本"作为基版本，用户无法指定"基于 v3 改，而不基于 v5"。

**方案**：
- `VersionCreate` schema 新增 `parent_version_id` 字段，支持三种语义：
  - `null`（不传）→ 默认行为，以 prompt 的 current_version 为基版本
  - `""`（空字符串）→ 空白新建，不基于任何版本
  - `uuid` → 以指定版本为基版本（必须是同 prompt 下的版本）
- 前端增加基版本下拉选择器，选中后自动填入对应版本内容到编辑区

#### 2. 模板选择

**问题**：创建 Prompt 时缺少引导，用户（尤其是新手）不清楚如何组织占位符、不知道常用 Prompt 结构长什么样。

**方案**：
- 前端新增 5 个预设模板，覆盖最常见场景：

| 模板 | 用途 | 占位符 |
|------|------|--------|
| 简单问答 | 通用对话、知识问答 | `{question}` |
| 上下文问答 (RAG) | 基于材料回答 | `{context}`, `{question}` |
| 文本分类 | 打标签、情感分析 | `{labels}`, `{text}` |
| 文本摘要 | 文章总结 | `{format}`, `{text}` |
| 代码生成 | 自然语言→代码 | `{language}`, `{requirement}` |

- 模板和基版本互斥：后选覆盖先选，避免内容来源混乱
- 可随时切回"自定义"自由编写

#### 3. 变量自动提取

**问题**：`variables` 字段需要用户手动填写，但它实际上可以从 Prompt 内容中自动解析——凡是 `{xxx}` 格式的占位符就是变量。手动填不仅麻烦，还容易和实际内容不一致。

**方案**：后端 `create_draft` 在 `variables` 为空数组时，自动用正则 `\{(\w+)\}` 从 content 中提取占位符名。用户不再需要在表单里填 variables（前端已移除该字段）。

---

### 级联安全删除（已有功能说明）

删除版本时，并不是简单的 `DELETE` 一行数据——可能破坏外键约束。当前实现的保护逻辑：

1. **解除 prompt 的 current_version_id 引用**：如果被删版本恰好是 prompt 的当前版本，先将 `prompt.current_version_id` 置为 null
2. **解除子版本的 parent_version_id 引用**：如果有其他版本以被删版本作为基版本，先将它们的 `parent_version_id` 置为 null
3. 以上两步完成后，才执行 delete

这保证了删除操作不会因为外键约束而报错，也不会产生悬空引用。
